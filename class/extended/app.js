'use strict'

class Ball {

   constructor(name) {
       this.name = name;
   }

   catchMonster() {
       console.log(`${this.name} threw.`);
   }
}

let monBall = new MonBall();
mokeBall.catchMonster();